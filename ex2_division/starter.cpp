// Exercise 2 — integer division (STARTER)
#include <iostream>
using namespace std;

int main()
{
    int a;
    int b;
    cout << "Enter a: ";
    cin >> a;
    cout << "Enter b: ";
    cin >> b;

    cout << "Integer division: " << (a / b) << endl;

    // TODO: print the real (decimal) result as:
    // Real division: 3.5
    // Hint: (double)a / b   or   a / 2.0 when b is 2

    return 0;
}
